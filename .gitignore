# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Data (never commit raw data)
data/raw/
data/processed/
data/interim/
!data/raw/**/.gitkeep
!data/processed/.gitkeep

# Unified test data can be created from raw data, so we ignore the large NC file but keep the provenance JSON
data/test/metocean_sc_sul_unified_waverys_grid.nc

# Figures and outputs
outputs/south_sc_test_data_exploratory/*

# Logs
logs/*.log
logs/download_status.json
!logs/.gitkeep
nohup.out
*/nohup.out

# User config (never commit — only the .example.yml is versioned)
config/download_config.yml
config/test_fixture.yml

# Test fixtures: commit only the small NC files, not provenance JSON
data/test/fixture_provenance.json

# Credentials
.copernicusmarine-credentials
.env
*.env

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

